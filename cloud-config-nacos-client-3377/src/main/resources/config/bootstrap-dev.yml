spring:
  application:
    name: nocos-config-client
  profiles:
    active: test
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yaml